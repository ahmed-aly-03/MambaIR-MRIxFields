# On top of the MambaIR environment (see MambaIR/README.md "Installation" for
# torch/CUDA/mamba_ssm/causal_conv1d, which need matching CUDA toolkit versions and
# are best installed by hand). This file only adds what the mri/ and train.py code
# needs beyond that.
nibabel>=5.1
numpy>=1.24
pyyaml>=6.0
tqdm>=4.66
scikit-image>=0.21   # evaluate.py: structural_similarity, matching the official evaluator's SSIM exactly
lpips>=0.1.4         # evaluate.py: LPIPS metric (downloads AlexNet weights on first use)
